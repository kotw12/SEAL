"""SEAL — Security Evolution from Automated Loop.

An autonomous web red-team orchestrator that runs an autonomous scan engine with a
verification loop (kill false positives) and an evolution loop (adapt the
attack strategy), targeting arbitrary authorized URLs over OpenRouter.
"""
__version__ = "0.1.0"
